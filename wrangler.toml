name = "ouroboros"
compatibility_date = "2024-09-01"
compatibility_flags = ["nodejs_compat"]

# KV namespace binding — replace "placeholder" with actual namespace ID after running:
#   npx wrangler kv:namespace create GAME_STATE
[[kv_namespaces]]
binding = "GAME_STATE"
id = "placeholder"
